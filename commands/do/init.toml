# Init (DeepOrbit prompt setup)

description = "Initialize or safely upgrade a DeepOrbit vault via do.init"
prompt = """
Use the do.init skill. Initialize the target in {{args}}, or the current directory when omitted. Report migration conflicts without overwriting either file.
"""
