# Fix Links (Ghost Link Fixer)

description = "Scan vault for ghost wikilinks, generate missing notes with first-principle content via do.fix-links"
prompt = """
Use the do.fix-links skill. Scan 40_知识库 for broken wikilinks, create missing files, fill with Wiki + first-principle content, and classify.
User input: {{args}}
"""
