# Agent CLI configuration (DeepOrbit)

description = "Detect, select, and inspect the local agent CLI via do.agent"
prompt = """
Use the do.agent skill. {{args}} — run `deeporbit --vault . agent detect`, ask the user which installed agent CLI to use (ask form when several), persist with `deeporbit --vault . agent configure <name> [--mode ...]`, or show the current configuration with `deeporbit --vault . agent status`.
"""
