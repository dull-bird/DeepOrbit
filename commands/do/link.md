---
description: "Link to an external DeepOrbit vault and route requests to it via do.link"
---

Use the do.link skill. $ARGUMENTS — link a vault path when one is given, otherwise resolve the target vault or use `deeporbit link route "<request>"` to pick the best match, then route the user's natural-language request to the workflows inside the linked vault.
