# DeepOrbit project context

@AGENTS.md
@DeepOrbitPrompt.md

## Claude Code integration

- The imported `DeepOrbitPrompt.md` is the canonical runtime context for this repository.
- Use the repository skills under `skills/` and the paired commands under `commands/do/`.
- Preserve vault Markdown as the authoritative user data; derived indexes remain local.
